

export interface GameConfig {
    BOARD_WIDTH: number;
    BOARD_HEIGHT: number;
    BOARD_DEPTH: number;
    PADDLE_WIDTH: number;
    PADDLE_HEIGHT: number;
    PADDLE_DEPTH: number;
    BALL_RADIUS: number;
    PADDLE_SPEED: number;
    BALL_SPEED: number;
}
// Fetched game config: {
// 	BOARD_WIDTH: 20, --->
// 	BOARD_HEIGHT: 0.1,
// 	BOARD_DEPTH: 15, --->
// 	PADDLE_WIDTH: 0.2, (--->)
// 	PADDLE_HEIGHT: 0.3,
// 	PADDLE_DEPTH: 5, --->
// 	BALL_RADIUS: 0.3,
// 	PADDLE_SPEED: 0.3,
// 	BALL_SPEED: 0.2

